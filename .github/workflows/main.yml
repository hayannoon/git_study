name: Python Syntax Check

on:
  push:
    branches:
      - main  # 'main' �귣ġ�� Ǫ�ð� �� ������ �����

jobs:
  check-syntax:
    runs-on: ubuntu-latest  # Ubuntu ȯ�濡�� ����

    steps:
      - name: Checkout code
        uses: actions/checkout@v2  # �ڵ� ����Ҹ� üũ�ƿ�

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.8'  # Python 3.8 ���� ���

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip  # pip �ֽ�ȭ
          pip install flake8  # flake8 ��ġ (Python ���� �˻� ����)

      - name: Check Python syntax with flake8
        run: |
          flake8 .  # ���� ���丮�� Python ���ϵ鿡 ���� flake8 ����

      - name: Push success message
        run: echo "No syntax errors found!"  # ���� ������ ������ �޽��� ���

